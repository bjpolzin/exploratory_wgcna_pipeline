## Indicate categorical group selection based on sample ID
categ_bool <- T # Categorial boolean: boolean value (True or False) indicating 
qual <- "mpoa" # Qualifier: string containing sample qualifier found in the sample ID names (e.g. tissue type, brain region, etc.)
group_1 <- "low"# First comparison group: string of first comparable group in sample ID (e.g. control)
group_2 <- "high" # Second comparison group: string of second comparable group in sample ID (e.g. exp)